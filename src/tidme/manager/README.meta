title: $:/plugins/keepone/tidme/manager/readme
type: text/vnd.tiddlywiki

! Tidme Manager

全局学习资产管理组件（对标 SuperMemo 的 Contents 知识树 / Browser 子集浏览器 / Registry）：

- **卡片管理器**（`<$card-manager/>`）：视图过滤（全部/在队/已读/搁置/逾期）× 组织方式
  （按文档 / 按牌组含未入组 / 列表 Browser 式可排序预览）× 单卡与批量操作
- **卡片信息面板**：列表列（状态/类型/优先级/标题/牌组/到期/间隔/难度/重复/遗忘）+ 详情预览全字段
- **单卡参数编辑**（对标 Element parameters）：改 due / 优先级 / 注释
- **侧边栏卡片树**（`<$card-browser/>`）：牌组 → 文档 → 卡片
- **牌组批量操作**（`<$queue-ops/>`）：顺延/提前/忽略/搁置/恢复/遗忘 + 立即顺延（auto-postpone 手动触发）

依赖 `$:/plugins/keepone/tidme/core`；不依赖 import/read（卡片管理是全局组件，与导入渠道无关）。
