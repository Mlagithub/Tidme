# SuperMemo 增量阅读调度机制与流程说明文档

适用对象：SuperMemo 增量阅读（Incremental Reading）使用者
核心场景：处理海量导入材料（如 10 本书、50 篇论文、100 篇网页文章）

## 1. 概述

在 SuperMemo 中，系统不会让用户像传统阅读那样线性地“读完一本再读下一本”。相反，它通过增量阅读（Incremental Reading） 和优先级队列（Priority Queue） 机制，将海量材料打碎、转化，并智能地调度到用户每天的学习任务中。
本文档详细解析 SuperMemo 决定“下一个内容是什么”的底层逻辑，并提供海量材料导入后的标准调度流程示例。

## 2. 内容的生命周期：调度的基本单位

SuperMemo 调度的不是“整本书”，而是随着用户的处理不断演化的元素（Elements）。导入的原始材料会经历以下三个阶段的转化：

1. 文章/主题（Articles / Topics）：
    * 定义：导入的原始材料（如整篇论文、网页文章）。
    * 状态：被动阅读的文本，用于泛读和寻找价值点。

2. 摘录/片段（Extracts）：
    * 定义：在阅读文章时，使用快捷键（如 Alt+X）提取出的重要段落或句子。
    * 状态：每个摘录都会成为一个独立的“微型文章”，继承母文章的优先级，并重新进入调度队列等待后续处理。

3. 知识点（Items）：
    * 定义：从摘录中进一步提炼出的问答对（Q&A）或完形填空（Cloze）。
    * 状态：用于长期记忆的最终形态，受 SuperMemo 间隔重复算法的严格保护。

## 3. 调度引擎：决定“下一个出现什么”

当用户点击“学习（Learn）”时，SuperMemo 会从待办队列（Outstanding Queue） 中决定下一个展示的内容。这个顺序由以下 4 个核心因素共同决定：

### 3.1 优先级（Priority, 0% ~ 100%）

* 规则：0% 代表最高优先级，100% 代表最低优先级。
* 作用：在默认设置下，系统每天会自动将待办队列按优先级从高到低进行排序（Auto-sort）。高优先级的论文片段，会永远优先于低优先级的网页文章出现。

### 3.2 间隔/到期日（Interval）

* 规则：只有到达预定复习日期的元素，才会进入当天的待办队列。
* 作用：间隔和优先级共同决定了复习的顺序。对于新导入的文章，系统会根据初始间隔排队；对于已转化为知识点（Items）的内容，算法会根据记忆反馈精确计算下次出现的日期。

### 3.3 元素类型配比（Proportion of Topics vs. Items）

* 规则：系统允许设置“主题（阅读材料）”与“知识点（复习卡片）”在每日队列中的比例。
* 作用：防止用户被源源不断的新文章吸引，而荒废了旧知识的复习。例如，系统可配置为每展示 1 篇新文章，就穿插展示 4 个需要复习的知识点，保证知识转化的闭环。

### 3.4 随机化（Randomization）

* 规则：在严格排序中引入一定程度的随机打乱。
作用：对抗人类的“优先级偏见”（总觉得新导入的材料比旧材料重要）。随机化让用户偶尔能“意外”回顾到之前被埋没但实际价值很高的旧材料。

## 4. 调度流程示例：160份材料的处理全纪录

初始设定：导入 10 本书、50 篇核心论文、100 篇背景网页文章（共 160 份材料）。

### 阶段一：导入与初始赋权
* 操作：将材料批量导入 SuperMemo。
* 调度逻辑：根据材料价值分配初始优先级。例如：核心专业论文设为 5%（高优），背景科普网页设为 80%（低优）。此时它们全部作为 Articles 进入优先级队列。

### 阶段二：每日交替阅读（跳跃式处理）
* 操作：用户开始当天的学习。
* 调度逻辑：
    1. 系统调出优先级最高且已到期的论文 A。
    2. 用户阅读了两段，发现一段话极具价值，按下 Alt+X 将其提取为 Extract。该 Extract 继承论文 A 的优先级，被安排在未来的某一天复习。
    3. 用户按下回车（Enter）。此时，系统不会让用户继续读论文 A 的第三段，而是根据优先级队列，直接切换到另一篇高优先级的材料，或一个需要复习的旧知识点。
* 结果：用户是在 160 份材料及其衍生的成千上万个片段之间进行“跳跃式”阅读。

### 阶段三：长期复习与知识固化
* 操作：时间推移，提取的数百个 Extracts 被逐步转化为 Items（完形填空）。
* 调度逻辑：这些 Items 严格受间隔重复算法保护。即使几个月没读某本书的原始文章，只要从中提取的知识点优先级够高，系统仍会在其即将被遗忘的临界点，精准地推送到用户面前。

## 5. 系统过载保护机制

导入 160 份材料极易导致“学习过载（Overload）”。SuperMemo 提供以下自动化机制保护学习节奏：

*  自动排序（Auto-sort repetitions）
    每天开始学习前，自动将待办队列按优先级排序，确保高价值内容优先处理。
*  自动推迟（Auto-postpone）
    如果当天时间不够，无法完成所有待办任务，系统会自动将低优先级的元素推迟到未来。这确保了用户每天面对的都是在当前优先级下最重要的内容，且不会让高优先级内容被过度延迟。

## 6. 实操最佳实践建议

*  诚实分配优先级：导入时切忌将所有材料都设为高优先级。必须严格区分“必须掌握的核心论文”和“仅需泛读的网页文章”。降级（Deprioritization）是增量阅读成功的关键。
*  开启自动化调度：在设置中务必勾选 Auto-sort repetitions 和 Auto-postpone，让算法替你管理每天的队列顺序，减轻认知负担。
*  拥抱“中断”：增量阅读的精髓在于“中断”。读几段、提取重点、然后交给系统调度。信任算法会在最合适的时间把内容再次推到你面前，不要试图一次性线性读完一本书。

* 监控优先级保护（Priority Protection）：定期查看统计面板中的 Priority protection 指标。如果该指标下降，说明你导入了太多高优先级材料导致系统过载，此时需要对部分材料果断进行“降级”处理。